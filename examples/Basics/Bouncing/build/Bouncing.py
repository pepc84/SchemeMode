# Generated by SchemeMode — Bouncing
from mewnala import *
from mewnala.math import *
import math as _math

def _err(*a): raise Exception(' '.join(str(x) for x in a))

import random as _random
def random(*args):
    if len(args)==1: return _random.uniform(0, args[0])
    return _random.uniform(args[0], args[1])

def random_gaussian(): return _random.gauss(0, 1)

def noise(*args): return _random.random()

# scm:3
x = 300.0

y = 200.0

# scm:4
vx = 3.5

vy = 2.2

# scm:5
r = 25.0

# scm:7
def setup():
    return size(600, 400)

# scm:9
def draw():
    global x
    global y
    global vx
    global vy
    # scm:10
    background(20)
    # scm:11
    x = (x + vx)
    y = (y + vy)
    # scm:12
    if (((x + r) > width) or ((x - r) < 0)):
        vx = (-vx)
    # scm:13
    if (((y + r) > height) or ((y - r) < 0)):
        vy = (-vy)
    # scm:14
    fill(80, 180, 255)
    no_stroke()
    # scm:15
    return ellipse(x, y, (2 * r), (2 * r))


run()
